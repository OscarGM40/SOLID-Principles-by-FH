"use strict";
// una tupla es como un arreglo (de echo es a lo que va a compilar en Javascript), solo que le indicamos cuantos items tendrá y de qué tipo al definirla.Reglas que habrá que cumplir al instanciarla
let tupleA;
tupleA = [1, 'a'];
let tupleB;
tupleB = ['', true, 1];
//obviamente dado que son un array puedo acceder por indice
console.log(tupleA[1]);
//usaremos tuplas en el patrón Adapter y Facade
